{
  "type": "function",
  "name": "page_create",
  "description": "Create one new unpublished static-page workspace with index.html, page.css, page.js, and assets/. Use a lowercase page slug. It never publishes the page and conflicts if the page already exists.",
  "parameters": {
    "type": "object",
    "properties": {
      "page_id": {"type": "string", "pattern": "^[a-z0-9](?:[a-z0-9-]{0,78}[a-z0-9])?$", "description": "New lowercase page slug."},
      "title": {"type": "string", "maxLength": 200, "description": "Optional HTML-escaped starter title."}
    },
    "required": ["page_id"],
    "additionalProperties": false
  },
  "strict": true
}
