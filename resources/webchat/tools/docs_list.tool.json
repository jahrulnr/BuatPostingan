{
  "type": "function",
  "name": "docs_list",
  "description": "List all documents in the internal docs corpus with their path, title, language, domain, and headings. Use to discover available documentation before searching or reading. Optionally filter by language and/or domain. Returned paths are internal metadata and are not user-accessible.",
  "parameters": {
    "type": "object",
    "properties": {
      "language": {
        "type": "string",
        "enum": ["id", "en", "unknown"],
        "description": "Optional documentation language filter."
      },
      "domain": {
        "type": "string",
        "description": "Optional module/domain filter, such as writing or architecture."
      },
      "limit": {
        "type": "integer",
        "minimum": 1,
        "maximum": 100,
        "description": "Maximum number of documents to return. Default 50."
      }
    },
    "required": [],
    "additionalProperties": false
  },
  "strict": true
}
