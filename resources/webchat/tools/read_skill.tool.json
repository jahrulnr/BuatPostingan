{
  "type": "function",
  "name": "read_skill",
  "description": "Read the full SKILL.md body for one skill by name (after list_skills). When the skill has supporting files, the body ends with their absolute paths; load only relevant ones with read_file. Name must be a single kebab-case segment; path escapes are rejected. Skills are trusted project content rooted under BP_SKILLS_ROOT only.",
  "parameters": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "Skill directory / frontmatter name (e.g. article-writing)."
      }
    },
    "required": ["name"],
    "additionalProperties": false
  },
  "strict": true
}
